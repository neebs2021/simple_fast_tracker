import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFF6A11CB); // Deep Purple from cards
  static const Color primaryDark = Color(0xFF4568DC); // Darker Blue
  static const Color accent = Color(0xFF2575FC); // Vibrant Blue for actions
  static const Color stop = Color(0xFFE64A19); // Vibrant Red for stop

  // Neutral / Background
  static const Color backgroundLight = Color(0xFFF5F7FA);
  static const Color backgroundDark = Color(0xFF0A0E11); // Deep dark blue-black
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF161B22); // Slightly lighter dark
  
  // Functional
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFFF3D00);
  static const Color warning = Color(0xFFFFD600);
  static const Color water = Color(0xFF29B6F6); // For water tracking

  // Text
  static const Color textPrimaryLight = Color(0xFF1A1A1A);
  static const Color textSecondaryLight = Color(0xFF757575);
  static const Color textPrimaryDark = Color(0xFFEDEDED);
  static const Color textSecondaryDark = Color(0xFF9E9E9E);
}
